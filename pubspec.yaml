name: http_client
description: |
    Wrapper of the http package to provide a single interface for both
    browser and console clients, until it will support it out of the box.
version: 0.1.0+1
author: Istvan Soos <istvan.soos@gmail.com>

homepage: https://github.com/isoos/http_client

environment:
  sdk: '>=1.20.0 <2.0.0'

dependencies:
  http: ^0.11.3
